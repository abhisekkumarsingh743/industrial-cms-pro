Clone the Repo

Local Setup:

Navigate to backend and run npm install && npm start.

Navigate to frontend and run npm install && npm start.

Docker Setup: Run docker-compose up --build from the root.

Deployment: Push to GitHub to trigger Vercel and Render auto-builds.